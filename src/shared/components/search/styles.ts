import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  search: {
    alignItems: 'center',
    flexDirection: 'row',
    columnGap: 16,
  },
  input: {
    flex: 1,
    borderWidth: 1,
    borderColor: 'lightgray',
    height: 50,
    borderRadius: 16,
    padding: 16,
  },
});
